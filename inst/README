Package 'subselect' requires the Fortran LAPACK library.

The configure script for this package attempts to detect the lapack
library and the BLAS libraries.

Details of changes since Version 0.1 are detailed in file CHANGELOG
